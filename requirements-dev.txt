-r requirements.txt
black==22.6.0   # Pinned to match pre-commit config
flake8==5.0.4   # Pinned to match pre-commit config
isort==5.10.1    # Pinned to match pre-commit config
mock
mutmut
pre-commit
pytest
pytest-cov
tox==4.0.8  # tox 4 seems to be broken at the moment
tox-pyenv
