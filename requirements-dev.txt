-r requirements.txt
black==22.6.0   # Pinned to match pre-commit config
flake8==4.0.1   # Pinned to match pre-commit config
isort==5.10.1    # Pinned to match pre-commit config
mock
mutmut
pre-commit
pytest
pytest-cov
tox
