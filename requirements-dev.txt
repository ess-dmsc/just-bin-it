-r requirements.txt
black==22.12.0   # Pinned to match pre-commit config
flake8==5.0.4   # Pinned to match pre-commit config
isort==5.11.1    # Pinned to match pre-commit config
mock
mutmut
pre-commit
pytest
pytest-cov
tox==3.27.1  # tox 4 seems to be broken at the moment
tox-pyenv
